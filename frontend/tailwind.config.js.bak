/** @type {import('tailwindcss').Config} */
export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {
            colors: {
                brand: {
                    dark: '#0B0F19',    // Midnight
                    primary: '#3B82F6', // Vibrant Blue
                    accent: '#10B981',  // Emerald
                    surface: 'rgba(255, 255, 255, 0.08)',
                }
            },
            fontFamily: {
                sans: ['"Outfit"', 'sans-serif'],
            },
            backdropBlur: {
                xs: '2px',
            }
        },
    },
    plugins: [],
}
